<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lzh.dao.UserDao">
    <select id="getTotal" resultType="int">
        select count(*) from t_user
    </select>
    <select id="queryUsers" resultType="User">
        select id,username,password,gender,register_time
        from t_user
    </select>
    <select id="queryUserById" resultType="User">
        select id,username,password,gender,register_time
        from t_user
        where id=#{id}
    </select>
    <update id="updateUser" parameterType="User">
        update t_user
        <trim prefix="set" suffixOverrides=",">
            <if test="username!=null">username=#{username},</if>
            <if test="password!=null">password=#{password},</if>
            <if test="gender!=null">gender=#{gender},</if>
            <if test="register_time">register_time=#{register_time}</if>
        </trim>
        where id=#{id}
    </update>
    <insert id="insertUser" parameterType="User">
        <selectKey keyProperty="id" order="AFTER" resultType="int">
            select last_insert_id()
        </selectKey>
        insert into t_user values (#{id},#{username},#{password},#{gender},#{register_time})
    </insert>
    <delete id="deleteUser" parameterType="int">
        delete from t_user where id=#{id}
    </delete>
    <insert id="insertManyUsers" parameterType="User">
        insert into t_user values
        <foreach collection="list" open="" close="" separator="," item="user1">
            (null,#{user1.username},#{user1.password},#{user1.gender},#{user1.register_time})
        </foreach>
    </insert>
    <delete id="deleteManyUsers" parameterType="int">
        delete from t_user where id in
        <foreach collection="list" open="(" close=")" separator="," item="id1">
            #{id1}
        </foreach>
    </delete>
</mapper>